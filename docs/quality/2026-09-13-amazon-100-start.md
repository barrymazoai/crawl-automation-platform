# Amazon 100-product background campaign

The user authorized 100 Amazon products to run while they sleep. A new immutable campaign started on Mini at 2026-09-13 15:52:27 UTC, using the normal Brand intake and existing Temporal product/label workflows. This is a launch record, not a claim that all 100 products have finished.

- Workflow ID: `amazon-history-100-us-10001-20260913`.
- Initial run: `01a09b78-4a5f-7047-b670-48d4ca5e4781`.
- Exactly 100 distinct selected ASINs, 100 immutable single-product requests.
- Region: Amazon US, delivery ZIP: New York 10001.
- Manifest SHA-256: `5430415d294ea96af1fe3ab9b42eba3894b800629e7f71c62ef732cfc5136ae9`.
- Mini data root: `/Users/barry/apps/crawlv3-history-20260913/amazon-100-us-20260913`.

Selection starts from the existing 2,000-product imported candidate list, excludes the 11 ASINs already discovered by previous Amazon tasks, and prefers existing nonempty USD price observations while retaining deterministic candidate order. All 100 selected products have a historical USD quote. Their legacy assignment group is Nature's Bounty; that historical company grouping is not a new assertion that every live product belongs to that brand.

The private source configuration preserves its 512 existing link batches and appends 100 new ones. Seven Amazon Activity Workers and the two existing campaign Workers restarted individually to load configuration. Actual release bytes and expected runtime build IDs were checked before reuse. The repaired Amazon Activity build remains `b3bb8e7bd190c0bb05e9fee157da692a042c8fb940c71b3d7224f069f650bb15`; no business source or Workflow code was changed for this campaign.

Main and campaign monitors retain PIDs 68275 and 67816. Unrelated Worker PIDs are unchanged. All 90 main services and both campaign services were ready at launch and first inspection. Credentials/configuration remain private on Mini.

The existing campaign queue name contains `10`, but its consumer now loads the new 100-product manifest. The manifest, workflow identity and checked product count define the limit. The completed five-product and CoQ10 acceptance workflows remain completed; the old ten-/2,000-product campaigns remain cancelled.

Temporal owns iteration, durable cursor, waiting and ContinueAsNew after 20 chunks. The launcher exits after starting the campaign; collection does not depend on this chat or a MacBook shell process. The usual review outcome is retained before moving to the next settled product. Unconfirmed execution/recovery pauses the batch instead of skipping ownership checks or inventing replacement requests. Normal browser work closes its exact pages before handoff; historical Reviews, source records and R2 evidence are preserved.

First inspection at 15:53:59 UTC confirmed the campaign RUNNING, phase `waiting-for-products`, one accepted request and one retained capture. First ASIN `B0C296MRW4` was running `acquireAmazonFile`. Its current quote was USD 7.99 versus a historical USD 8.99; both observations remain available. Historical purchase/delivery conditions are incomplete, so this is a page-quote comparison, not an identical-condition transaction-price comparison.

Inspect the background campaign on Mini with:

```sh
/opt/homebrew/bin/node /Users/barry/apps/crawlv3-history-20260913/amazon-100-us-20260913/inspect.mjs
```

Progress and prices are retained in `temporal-progress.json`, `temporal-prices.json` and `inspection.json` under the data root. The campaign remains in the background; this launch does not mark its products collected or its terminal cleanup complete.

[Launch receipt](evidence/2026-09-13-amazon-100/started.json), [selection summary](evidence/2026-09-13-amazon-100/selection-summary.json), [first live inspection](evidence/2026-09-13-amazon-100/first-inspection.json).
