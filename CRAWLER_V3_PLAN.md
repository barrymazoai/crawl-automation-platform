# 爬虫 V3 重构计划 · Temporal 版

更新：2026-09-05 · 讨论收口版。状态：同步网络、资源、入库与代码契约后的文档评审稿；没有安装框架、改动业务实现或切换生产。

- [完整设计与验收矩阵](docs/spark/2026-09-05-crawler-v3-temporal-design.md)
- [可离线打开的 HTML 架构图](docs/crawler-v3-architecture.html)
- 旧 `PARALLEL_CRAWL_PIPELINE_PLAN.md` 仅保留历史背景，不作为 V3 实施依据。

## 1. 最终方向

Temporal 管流程、队列和执行历史；编排 Worker 跑流程代码；各原子模块 Worker 独立启动、主动拉取任务。主机运行层管机器；每模块配置并发，实际共享资源才启用共同限额，不默认建设独立资源服务。业务库与 R2 保存数据和证据。Temporal UI 作为执行看板，自有 Dashboard 只做业务汇总、必要只读 Review 和可选节点健康摘要。

不是给旧 Job 队列叠一层 Temporal，也不是把 OCR / Codex / 抓取装进一个巨型 Worker。

V3 独立建设入口、配置、编排、契约和测试环境。旧解析器、客户端、存储工具与纯函数仅作复用候选：审核隐藏依赖和副作用、通过新契约测试后才采用；旧成功不等于正确，不带入旧重试 / 批处理 / 清理 / Job 生命周期。

## 2. 不可改变的约束

| 项目 | V3 规则 |
| --- | --- |
| 原子模块 | 独立配置、启动、部署；OCR 一次一个文件 |
| 并行 | 产品之间流水；同产品按依赖等待；同一浏览器会话内导航 / 切规格串行 |
| Worker 释放 | 本步骤可靠交接完成即可接单，不等下游 |
| 资源不足 | 未执行任务等资源，不在 Worker 内先领取一堆任务 |
| 共享限额（按需启用） | 跟随实际主机 / 账号 / 会话 / 出口资源池；独立资源可独立运行，换电脑不自动增加同账号额度 |
| 失败与恢复 | 不自动重跑业务；验证完成凭证、只补交接；失败分类留在被动 Review |
| 文件 | 同机可访问且校验通过用缓存，否则 R2；R2 不随成功删除 |
| PDF | 独立跨平台模块，推荐 PDFium / pypdfium2；不依赖 Swift / PDFKit |
| 网络默认 | Codex 沿用默认网络，不切节点；美国 Windows DTC 默认不管理网络，继承主机环境，不是强制直连 |
| 网络适配 | 静态代理、Clash、ScraperAPI 按能力选用；专用组只在授权范围及同组安全边界轮动，不影响其他流量 |
| 数据入库门槛 | Formula + Ingredients 真实齐全；价格等可选项不阻塞；公司归属未匹配 / 歧义 / 冲突独立进 Review |
| 写入保护 | 正常 SKU 独立推进；首版全局串行；写入和回读分开；不随共享资源控制的关闭而取消 |
| 下线查询 | 独立流程，基于目录发现集合，不基于入库集合；不自动下架 |
| 历史任务 | 成功样本建立隔离验证副本；旧失败保留分类，不迁移续跑 |
| 重复与新采集 | 同操作重复先核验证据；主动新采集建立新 observation，不仅按 URL 复用旧结果 |
| 启停与版本 | 先停接单再有界收尾；未知操作不重跑；实际版本可追溯，不兼容消费者不接单 |

代码按功能组织，TypeScript strict、函数优先、ports + adapters；配置在启动时校验，工厂创建依赖，DI 注入普通业务函数。Awilix 仅为待最小验证的组装层候选：每 Worker 独立容器，任务状态隔离，显式释放，不进入业务核心或 Workflow。完整模式、模块契约和身份规则见设计第 5 节。

## 3. 分阶段交付

以下均是待执行的交付门，不是已完成进度。通过退出条件再进入下一阶段，生产准入另行批准。

### P0 — 环境与外部契约验证

交付：隔离 Temporal 开发环境、最小编排与模拟 Activity；Awilix 候选的构建 / 依赖隔离 / 退出验证；Mac / Windows SDK 与浏览器联动验证；现有环境中的 PDF 样本验证（未来服务器上线前补对应环境验收）；实际产品服务单 SKU partial 提交、回读、幂等、安全收口的测试契约报告；锁定版本清单。

第一个验证切片：一个文件引用 → 模拟 OCR → 模拟保存结果及完成凭证 → 下游读取。先验证重复投递、断线、退出和状态隔离，不调用真实 OCR / Codex / 生产写入；P1 接测试 R2 与业务登记，P2 再经批准接真实 OCR。模拟通过不等于真实服务验收通过。

退出条件：跨机模拟执行可复现，退出与恢复可解释，兼容性和产品契约有证据。生产托管 / 自建选型另行批准；没有通过的环境不能接生产任务。

### P1 — 原子契约、产物与交接

交付：request / operation / observation 身份、ArtifactRef、输入指纹、版本化结果 / 错误契约；测试 R2、本地优先 Resolver、完成清单、幂等登记与核验；薄 Worker 入口、作用域与启停；被动 Review；入口 / 结果窄范围可靠投递。

退出条件：A01、A04–A09、A18、A24、A28–A30 通过。完成回执丢失不增加昂贵处理次数；新观察不冒用旧抓取；证据不完整安全留 Review；没有第二套调度状态机。真实公司匹配在 P2 验收。

### P2 — 单产品处理闭环

交付：独立文件、PDF、单文件 OCR、Codex 文本 / 视觉、标准化、校验、归属模块；Formula + Ingredients 门槛及公司错误分类；ProductWorkflow 证据路径和 family 边界；只连接测试目标的写入 / 回读适配器；版本化证据规则及人工确认样例。真实模型调用限定在另行批准的凭证、样本和调用范围。

退出条件：单产品从证据到测试入库或 Review 可复现；A02、A03、A08、A14、A15、A23 通过；PDF 页图身份关联正确；可选字段 / 可选支线失败不偷偷增加入库条件。

### P3 — 渠道、网络与资源隔离

交付：独立 V3 Amazon / GNC / Swanson / DTC 抓取模块与旧组件复用审查；持续发布发现项的 CatalogWorkflow 与独立 PresenceWorkflow；不管理网络 / 静态代理 / Clash / ScraperAPI 适配；按实际共享关系启用的资源协调、全局串行写入保护、背压与健康检查。

退出条件：A10–A14、A16、A19、A20、A25–A27、A31、A32 通过。共享资源不超额，独立资源不误互锁；不互切出口、不出现双写者。Codex 默认路由不变、Windows 网络管理可关闭。轮动只在授权测试资源验证，现有共享 Clash 配置不作为试验场。

### P4 — 历史验证与可观察性

交付：历史成功任务的隔离验证副本；历史失败分类记录，不自动执行；Temporal UI 搜索属性和跳转；真实业务 Dashboard；必要只读 Review 和可选节点摘要；回放、时间快进与故障注入报告。

退出条件：A17、A21、A22 通过；原失败不覆盖；Dashboard 不把 Completed / Review 混算入库成功。

### P5 — 受控生产切换

交付：全验收矩阵报告、生产配置、保留策略检查、停止接单与回滚方案；用户批准后停止旧接单、核验在途副作用；首批有限的新 V3 请求。

退出条件：无关键安全问题；R2 保留、全局写许可和 partial 语义可验证。回退只停止 V3 新接单并保留产物，不自动恢复旧失败，不撤销已确认产品写入。

## 4. 第一版不做

- 通用 DAG 编辑器、第二套调度 / 租约引擎、另一套任务操作看板。
- 完整远程机器控制平台、自动购买服务器或未经批准的收费部署。
- Target / Walmart 等新渠道、自动下架 / 停产判断。
- 自动修复 Review、失败后自动换模型 / 代理重跑、成功清理 R2。
- 为兼容旧流程而保留旧大 Worker 或旧 Job 续跑逻辑。

## 5. 本轮文档交付

本轮仅交付新版计划、完整设计和交互架构图。用户复核后再授权进入 P0；文件存在、图中列出模块、计划列出验收，都不表示这些运行能力已经实现。

本版文档验证：HTML 七个视图（新增代码与契约）、27 个模块按钮、四种交接场景、九类错误展开均已检查；1280 / 390 / 360 像素下全部视图无横向溢出，桌面代码视图与窄屏代码 / 网络视图已截图复核。键盘方向键 / Home / End、`#code` 深链接、本地链接、ARIA 引用、脚本语法和唯一 ID 已检查，无页面或控制台错误。深链接导航命令曾超时，随后确认页面已完整加载且选中正确视图。

完整系统验收扩展为 A01–A32，仍全部是后续交付门；以上是文档交互验证，不是系统验收，也没有运行真实 OCR、Worker 或生产写入。
