# 爬虫 V3 重构计划 · Temporal 版

更新：2026-09-05。状态：本轮设计与分阶段计划已整理，等待文档评审；没有安装框架、改动业务实现或切换生产。

- [完整设计与验收矩阵](docs/spark/2026-09-05-crawler-v3-temporal-design.md)
- [可离线打开的 HTML 架构图](docs/crawler-v3-architecture.html)
- 旧 `PARALLEL_CRAWL_PIPELINE_PLAN.md` 仅保留历史背景，不作为 V3 实施依据。

## 1. 最终方向

Temporal 管流程、队列和执行历史；编排 Worker 跑流程代码；各原子模块 Worker 独立启动、主动拉取任务。机器管理和共享资源管理独立于 Temporal。业务库与 R2 保存数据和证据。Temporal UI 作为执行看板，自有 Dashboard 只做业务汇总、必要只读 Review 和可选节点健康摘要。

不是给旧 Job 队列叠一层 Temporal，也不是把 OCR / Codex / 抓取装进一个巨型 Worker。

## 2. 不可改变的约束

| 项目 | V3 规则 |
| --- | --- |
| 原子模块 | 独立配置、启动、部署；OCR 一次一个文件 |
| 并行 | 产品之间流水；同产品按依赖等待；同一浏览器会话内导航 / 切规格串行 |
| Worker 释放 | 本步骤可靠交接完成即可接单，不等下游 |
| 资源不足 | 未执行任务等资源，不在 Worker 内先领取一堆任务 |
| 失败与恢复 | 不自动重跑业务；验证完成凭证、只补交接；失败分类留在被动 Review |
| 文件 | 同机可访问且校验通过用缓存，否则 R2；R2 不随成功删除 |
| PDF | 独立跨平台模块，推荐 PDFium / pypdfium2；不依赖 Swift / PDFKit |
| 网络 | 静态代理、Clash、ScraperAPI 能力匹配；会话出口隔离 |
| 入库 | 正常 SKU 独立推进；首版全局串行；写入和回读分开 |
| 下线查询 | 独立流程，基于目录发现集合，不基于入库集合；不自动下架 |
| 历史任务 | 成功样本建立隔离验证副本；旧失败保留分类，不迁移续跑 |

## 3. 分阶段交付

以下均是待执行的交付门，不是已完成进度。通过退出条件再进入下一阶段，生产准入另行批准。

### P0 — 环境与外部契约验证

交付：隔离 Temporal 开发环境、最小编排与模拟 Activity；Mac / Windows SDK 与浏览器联动验证；现有环境中的 PDF 样本验证（未来服务器上线前补对应环境验收）；实际产品服务单 SKU partial 提交、回读、幂等、安全收口的契约报告；锁定版本清单。

退出条件：跨机模拟执行可复现，退出与恢复可解释，兼容性和产品契约有证据。生产托管 / 自建选型另行批准；没有通过的环境不能接生产任务。

### P1 — 原子契约、产物与交接

交付：ArtifactRef、operation / observation ID、输入指纹、结果 / 错误契约；R2、本地优先 Resolver、完成清单、幂等登记与核验；薄 Worker 入口；被动 Review；入口 / 结果窄范围可靠投递。

退出条件：A01、A04–A09、A15、A18 通过。完成回执丢失不增加昂贵处理次数；证据不完整安全留 Review；没有第二套调度状态机。

### P2 — 单产品处理闭环

交付：独立文件、PDF、单文件 OCR、Codex 文本 / 视觉、标准化、校验、归属模块；ProductWorkflow 证据路径和 family 边界；只连接测试目标的写入 / 回读适配器；版本化证据规则及人工确认样例。

退出条件：单产品从证据到测试入库或 Review 可复现；A02、A03、A08、A14、A15 通过；PDF 页图身份关联正确。

### P3 — 渠道、网络与资源隔离

交付：全新 Amazon / GNC / Swanson / DTC 抓取模块；CatalogWorkflow 与 PresenceWorkflow；静态代理 / Clash / ScraperAPI Adapter；跨机资源许可、全局串行写入保护、背压与健康检查。

退出条件：A10–A14、A16、A19、A20 通过。两机运行不放大账号额度、不互切出口、不出现双写者。现有共享 Clash 配置不作为自动切换试验场。

### P4 — 历史验证与可观察性

交付：历史成功任务的隔离验证副本；历史失败分类记录，不自动执行；Temporal UI 搜索属性和跳转；真实业务 Dashboard；必要只读 Review 和可选节点摘要；回放、时间快进与故障注入报告。

退出条件：A17、A21、A22 通过；原失败不覆盖；Dashboard 不把 Completed / Review 混算入库成功。

### P5 — 受控生产切换

交付：全验收矩阵报告、生产配置、保留策略检查、停止接单与回滚方案；用户批准后停止旧接单、核验在途副作用；首批有限的新 V3 请求。

退出条件：无关键安全问题；R2 保留、全局写许可和 partial 语义可验证。回退只停止 V3 新接单并保留产物，不自动恢复旧失败，不撤销已确认产品写入。

## 4. 第一版不做

- 通用 DAG 编辑器、第二套调度 / 租约引擎、另一套任务操作看板。
- 完整远程机器控制平台、自动购买服务器或未经批准的收费部署。
- Target / Walmart 等新渠道、自动下架 / 停产判断。
- 自动修复 Review、失败后自动换模型 / 代理重跑、成功清理 R2。
- 为兼容旧流程而保留旧大 Worker 或旧 Job 续跑逻辑。

## 5. 本轮文档交付

本轮仅交付新版计划、完整设计和交互架构图。用户复核后再授权进入 P0；文件存在、图中列出模块、计划列出验收，都不表示这些运行能力已经实现。

文档验证：HTML 六个视图、27 个模块按钮、四种交接场景与九类错误展开已检查；桌面及 390 / 360 像素窄屏无横向溢出，键盘页签切换与本地链接已检查。这是文档交互验证，不是 A01–A22 的系统验收。
